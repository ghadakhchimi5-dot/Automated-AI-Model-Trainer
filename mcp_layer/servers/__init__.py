"""MCP servers — one process per agent. Each module is runnable standalone:

    python -m mcp_layer.servers.data_server

and can also be introspected with the MCP inspector:

    mcp dev mcp_layer/servers/data_server.py
"""
